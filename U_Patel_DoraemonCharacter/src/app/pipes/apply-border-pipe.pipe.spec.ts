import { ApplyBorderPipePipe } from './apply-border-pipe.pipe';

describe('ApplyBorderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyBorderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
